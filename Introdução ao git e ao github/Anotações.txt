
Link do download do git: https://git-scm.com/downloads




Comandos do Git:
git status = exibe situação atual
git add . = adiciona todos os arquivos para o commit
git add -A = adiciona todos os arquivos para o commit
git commit -m "msg para ficar registrada commit" = prepara commit para envio
git push origin main = envia commit para GitHub